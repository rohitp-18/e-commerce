import { createTheme } from "@mui/material";

const muiTheme = () => {
  return createTheme({
    palette: {
      primary: {
        // main : #1976d2
        main: "#ff6347",
      },
    },
  });
};

export default muiTheme;
